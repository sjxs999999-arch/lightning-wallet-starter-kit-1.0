import{describe,expect,it}from'vitest';import{normalizePair}from'./market.js';describe('market normalization',()=>{it('returns read-only public metrics without sensitive material',()=>{const result=normalizePair({baseToken:{address:'0x1',name:'Token',symbol:'TOK'},quoteToken:{symbol:'USDC'},priceUsd:'1.2',liquidity:{usd:900},txns:{h24:{buys:3,sells:2}}},'EVM');expect(result).toMatchObject({address:'0x1',priceUsd:1.2,liquidityUsd:900,readOnly:true});expect(JSON.stringify(result)).not.toMatch(/privateKey|mnemonic|signature/i)})});

import{describe,expect,it}from'vitest';import{createRuleSchema,maskDestination,safeWebhook,validateChannel}from'./automation.js';describe('automation security',()=>{it('accepts read-only alert rules and rejects unknown signing fields',()=>{expect(createRuleSchema.parse({name:'ETH price',kind:'price',scheduleMinutes:5,condition:{operator:'above',threshold:3000},channels:['email']})).toMatchObject({kind:'price'});expect(()=>createRuleSchema.parse({name:'Bad rule',kind:'price',scheduleMinutes:5,condition:{},channels:[],privateKey:'secret'})).toThrow()});it('blocks local and insecure webhooks',()=>{expect(safeWebhook('https://alerts.example.com/hook')).toBe(true);expect(safeWebhook('http://localhost/hook')).toBe(false);expect(safeWebhook('https://127.0.0.1/hook')).toBe(false)});it('validates and masks channel destinations',()=>{expect(validateChannel({name:'Ops',kind:'email',destination:'ops@example.com',enabled:false})).toBeTruthy();expect(maskDestination('ops@example.com','email')).toBe('op***@example.com')})});

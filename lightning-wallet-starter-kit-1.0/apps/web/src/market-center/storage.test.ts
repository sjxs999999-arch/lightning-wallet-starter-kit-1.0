import{beforeEach,describe,expect,it}from'vitest';import{addAlert,loadWatchlist,toggleWatch,triggered}from'./storage';
const values=new Map<string,string>();Object.defineProperty(globalThis,'localStorage',{value:{getItem:(key:string)=>values.get(key)??null,setItem:(key:string,value:string)=>values.set(key,value),clear:()=>values.clear()}});Object.defineProperty(globalThis,'crypto',{value:{randomUUID:()=> '00000000-0000-4000-8000-000000000000'}});
const token:any={chain:'SOL',address:'So11111111111111111111111111111111111111112',name:'Wrapped SOL',symbol:'SOL'};
describe('market local state',()=>{beforeEach(()=>localStorage.clear());it('stores only public watchlist metadata',()=>{toggleWatch(token);expect(loadWatchlist()).toEqual([{chain:'SOL',address:token.address,name:'Wrapped SOL',symbol:'SOL'}]);expect(localStorage.getItem('lightning-market-watchlist-v1')).not.toMatch(/privateKey|mnemonic/i)});it('evaluates local alerts',()=>{const alert=addAlert(token,'above',100)[0]!;expect(triggered(alert,101)).toBe(true);expect(triggered(alert,99)).toBe(false)})});

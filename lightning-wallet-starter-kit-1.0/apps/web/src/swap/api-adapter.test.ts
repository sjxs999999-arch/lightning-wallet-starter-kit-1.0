import{beforeEach,describe,expect,it,vi}from'vitest';import{fetchSwapCandidates}from'../../../api/src/swap';
describe('live swap adapters',()=>{beforeEach(()=>{vi.unstubAllGlobals();delete process.env.ZEROX_API_KEY;process.env.SWAP_PROVIDER_URLS=''});it('normalizes a Jupiter quote',async()=>{vi.stubGlobal('fetch',vi.fn(async()=>({ok:true,json:async()=>({inAmount:'100',outAmount:'250',otherAmountThreshold:'247',priceImpactPct:'0.001',routePlan:[{swapInfo:{label:'Raydium'}}]})})));const result=await fetchSwapCandidates({chain:'SOL',sellToken:'mint-a',buyToken:'mint-b',sellAmount:'100',taker:'11111111111111111111111111111111',slippageBps:50});expect(result[0]).toMatchObject({provider:'Jupiter',amountOut:'250',minReceived:'247',route:['Raydium']})});it('refuses fake EVM quotes when the provider is not configured',async()=>{await expect(fetchSwapCandidates({chain:'EVM',chainId:1,sellToken:'a',buyToken:'b',sellAmount:'1',taker:'0x0000000000000000000000000000000000000001',slippageBps:50})).rejects.toThrow('ZEROX_API_KEY')})});

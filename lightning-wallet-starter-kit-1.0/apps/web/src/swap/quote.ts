import type{SwapCandidate,SwapRequest}from'./types';export async function fetchQuotes(request:SwapRequest,timeoutMs=10_000):Promise<SwapCandidate[]>{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeoutMs);try{const api=import.meta.env.VITE_API_URL||'http://localhost:3001/api/v1',response=await fetch(`${api}/swap/quotes`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(request),signal:controller.signal}),body=await response.json();if(!response.ok)throw new Error(body?.message??body?.error??`Quote HTTP ${response.status}`);if(!Array.isArray(body.data)||!body.data.length)throw new Error('聚合器没有返回可用路线');return body.data}finally{clearTimeout(timer)}}

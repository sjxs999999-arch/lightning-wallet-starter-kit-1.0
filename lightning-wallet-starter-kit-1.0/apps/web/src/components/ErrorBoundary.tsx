import React from 'react';
export class ErrorBoundary extends React.Component<React.PropsWithChildren,{failed:boolean}> { state={failed:false}; static getDerivedStateFromError(){return{failed:true}} componentDidCatch(error:Error,info:React.ErrorInfo){const token=localStorage.getItem('lightning-session'),base=import.meta.env.VITE_API_URL||'http://localhost:3001/api/v1';if(token)void fetch(`${base}/errors/report`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${token}`},body:JSON.stringify({name:error.name.slice(0,100),message:error.message.slice(0,500),route:location.pathname.slice(0,200),stack:`${error.stack??''}\n${info.componentStack??''}`.slice(0,4000),release:'2.1.0'})}).catch(()=>undefined)} render(){return this.state.failed?<main className="fatal"><div><span className="logo-mark">ϟ</span><h1>页面暂时无法加载</h1><p>系统已保护其他功能不受影响。错误报告已脱敏记录，请刷新页面或返回控制台。</p><button onClick={()=>location.reload()}>重新加载</button><button onClick={()=>location.assign('/')}>返回控制台</button></div></main>:this.props.children} }

import type { LocalWalletRecord } from './types';
const download=(name:string,type:string,content:string)=>{const url=URL.createObjectURL(new Blob([content],{type}));const anchor=document.createElement('a');anchor.href=url;anchor.download=name;anchor.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};
export function exportPublicCsv(wallets:LocalWalletRecord[]){const esc=(v:string|number)=>`"${String(v).replaceAll('"','""')}"`;const csv=['name,chain,address,publicKey,derivationPath,index,createdAt',...wallets.map(w=>[w.name,w.chain,w.address,w.publicKey,w.path,w.index,w.createdAt].map(esc).join(','))].join('\n');download(`lightning-wallets-${Date.now()}.csv`,'text/csv;charset=utf-8',`\ufeff${csv}`)}
export function exportEncryptedJson(wallets:LocalWalletRecord[],salt:string){download(`lightning-wallets-encrypted-${Date.now()}.json`,'application/json',JSON.stringify({format:'lightning-wallet-encrypted-v1',algorithm:'AES-256-GCM',kdf:{name:'PBKDF2-SHA-256',iterations:600000,salt},wallets},null,2))}

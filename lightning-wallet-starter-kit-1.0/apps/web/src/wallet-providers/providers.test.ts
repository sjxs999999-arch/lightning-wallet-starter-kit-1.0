import{describe,expect,it}from'vitest';import{discoverEvmProviders}from'./providers';class TestCustomEvent<T>extends Event{detail:T;constructor(type:string,init:{detail:T}){super(type);this.detail=init.detail}}Object.defineProperty(globalThis,'window',{value:new EventTarget()});Object.defineProperty(globalThis,'CustomEvent',{value:TestCustomEvent});describe('wallet provider discovery',()=>{it('discovers EIP-6963 providers without reading keys',async()=>{const provider={request:async()=>[]};window.addEventListener('eip6963:requestProvider',()=>window.dispatchEvent(new CustomEvent('eip6963:announceProvider',{detail:{info:{name:'Rabby',rdns:'io.rabby'},provider}})),{once:true});const found=await discoverEvmProviders(0);expect(found[0]?.info.name).toBe('Rabby');expect(JSON.stringify(found[0]?.info)).not.toMatch(/privateKey|mnemonic/i)})});

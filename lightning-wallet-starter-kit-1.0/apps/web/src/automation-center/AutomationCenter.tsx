import{useCallback,useEffect,useState}from'react';import{Activity,Bell,Clock3,History,Mail,Play,RefreshCw,RotateCcw,Send,ShieldCheck,Webhook}from'lucide-react';import{api}from'../api';import{safeRulePayload}from'./guard';import type{AutomationJob,AutomationOverview,ChannelKind,Health,NotificationChannel,RuleKind}from'./types';
const kinds:Record<RuleKind,string>={price:'价格提醒',watchlist:'自选提醒',portfolio:'资产组合提醒',gas:'Gas 提醒',health:'健康监控'};const empty:AutomationOverview={rules:[],channels:[],jobs:[],retryQueue:0,deliveryEnabled:false};
export function AutomationCenter(){const[data,setData]=useState(empty),[health,setHealth]=useState<Health|null>(null),[tab,setTab]=useState<'rules'|'channels'|'history'>('rules'),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState('');const load=useCallback(async()=>{setError('');try{const[overview,status]=await Promise.all([api<{data:AutomationOverview}>('/automation/overview'),api<{data:Health}>('/automation/health')]);setData(overview.data);setHealth(status.data)}catch{setError('自动化服务暂时不可用，页面不会执行任何任务或交易')}},[]);useEffect(()=>{void load();const timer=setInterval(()=>void load(),30000);return()=>clearInterval(timer)},[load]);async function action(path:string,init?:RequestInit){setBusy(true);setError('');setNotice('');try{await api(path,init);setNotice('操作完成；当前仍为只读监控 / Dry Run');await load()}catch{setError('操作失败，未发送通知、未签名、未广播交易')}finally{setBusy(false)}}return <><div className="page-head"><div><p className="eyebrow">READ-ONLY OPERATIONS AUTOMATION</p><h1>自动化中心</h1><p>调度监控任务、通知通道、重试队列与完整执行历史。</p></div><button onClick={()=>void load()}><RefreshCw size={16}/>刷新状态</button></div><div className="automation-security"><ShieldCheck size={17}/>默认只读监控 · 通知默认 Dry Run · 无密钥 · 无自动签名 · 无交易广播</div><section className="automation-stats"><Stat label="调度规则" value={data.rules.length} icon={<Clock3/>}/><Stat label="启用规则" value={data.rules.filter(r=>r.enabled).length} icon={<Activity/>}/><Stat label="通知通道" value={data.channels.length} icon={<Bell/>}/><Stat label="重试队列" value={data.retryQueue} icon={<RotateCcw/>}/><Stat label="系统健康" value={health?.status??'检查中'} icon={<ShieldCheck/>}/></section><section className="panel automation-health"><span><i className={health?.api==='online'?'ok':''}/>API {health?.api??'—'}</span><span><i className={health?.postgres==='online'?'ok':''}/>PostgreSQL {health?.postgres??'—'}</span><span><i className={health?.scheduler==='running'?'ok':''}/>Scheduler {health?.scheduler??'—'}</span><span>延迟 {health?.latencyMs??'—'} ms</span><span>真实通知：{data.deliveryEnabled?'已启用':'关闭'}</span></section>{error&&<div className="batch-error automation-message">{error}</div>}{notice&&<div className="automation-notice">{notice}</div>}<div className="automation-tabs"><button className={tab==='rules'?'active':''} onClick={()=>setTab('rules')}>任务与提醒</button><button className={tab==='channels'?'active':''} onClick={()=>setTab('channels')}>通知中心</button><button className={tab==='history'?'active':''} onClick={()=>setTab('history')}>任务历史</button></div>{tab==='rules'&&<Rules data={data} busy={busy} action={action}/>} {tab==='channels'&&<Channels channels={data.channels} busy={busy} action={action}/>} {tab==='history'&&<Jobs jobs={data.jobs} busy={busy} action={action}/>}</>}
function Stat({label,value,icon}:{label:string;value:string|number;icon:React.ReactNode}){return <div className="panel automation-stat"><span>{icon}{label}</span><b>{value}</b></div>}
function Rules({data,busy,action}:{data:AutomationOverview;busy:boolean;action:(p:string,i?:RequestInit)=>Promise<void>}){const[name,setName]=useState('ETH 价格提醒'),[kind,setKind]=useState<RuleKind>('price'),[minutes,setMinutes]=useState(15),[threshold,setThreshold]=useState(3000),[chain,setChain]=useState('EVM');function submit(e:React.FormEvent){e.preventDefault();const payload=safeRulePayload({name,kind,schedule_minutes:minutes,condition:{chain,metric:kind==='gas'?'gasPrice':'price',operator:'above',threshold},channels:[]});void action('/automation/rules',{method:'POST',body:JSON.stringify(payload)})}return <div className="automation-grid"><form className="panel automation-form" onSubmit={submit}><h3>新建调度任务</h3><label>任务名称<input value={name} onChange={e=>setName(e.target.value)}/></label><label>监控类型<select value={kind} onChange={e=>setKind(e.target.value as RuleKind)}>{Object.entries(kinds).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><label>网络<select value={chain} onChange={e=>setChain(e.target.value)}><option>EVM</option><option>SOL</option><option>TRON</option></select></label><label>阈值<input type="number" value={threshold} onChange={e=>setThreshold(Number(e.target.value))}/></label><label>执行间隔（分钟）<input type="number" min="1" max="10080" value={minutes} onChange={e=>setMinutes(Number(e.target.value))}/></label><div className="notice"><ShieldCheck size={16}/>调度器只读取公开指标并写入任务历史，不调用钱包。</div><button disabled={busy}>创建只读任务</button></form><section className="panel automation-list"><div className="panel-head"><h3>Task Scheduler</h3><span>{data.rules.length} 条规则</span></div>{data.rules.map(rule=><div className="automation-row" key={rule.id}><span className={`automation-kind ${rule.kind}`}>{kinds[rule.kind].slice(0,2)}</span><div><b>{rule.name}</b><small>{kinds[rule.kind]} · 每 {rule.schedule_minutes} 分钟 · 下次 {new Date(rule.next_run_at).toLocaleString()}</small></div><label className="switch"><input type="checkbox" checked={rule.enabled} onChange={e=>void action(`/automation/rules/${rule.id}`,{method:'PATCH',body:JSON.stringify({enabled:e.target.checked})})}/><span/></label><button disabled={busy} title="立即 Dry Run" onClick={()=>void action(`/automation/rules/${rule.id}/run`,{method:'POST'})}><Play size={15}/></button></div>)}{!data.rules.length&&<p className="automation-empty">尚未创建自动化规则。</p>}</section></div>}
function Channels({channels,busy,action}:{channels:NotificationChannel[];busy:boolean;action:(p:string,i?:RequestInit)=>Promise<void>}){const[name,setName]=useState('运营通知'),[kind,setKind]=useState<ChannelKind>('telegram'),[destination,setDestination]=useState('');function submit(e:React.FormEvent){e.preventDefault();void action('/automation/channels',{method:'POST',body:JSON.stringify({name,kind,destination,enabled:false})});setDestination('')}return <div className="automation-grid"><form className="panel automation-form" onSubmit={submit}><h3>Webhook Manager</h3><label>通道名称<input value={name} onChange={e=>setName(e.target.value)}/></label><label>通道类型<select value={kind} onChange={e=>setKind(e.target.value as ChannelKind)}><option value="telegram">Telegram</option><option value="email">Email</option><option value="webhook">Webhook</option></select></label><label>{kind==='telegram'?'Chat ID':kind==='email'?'Email':'HTTPS Webhook URL'}<input value={destination} onChange={e=>setDestination(e.target.value)} placeholder={kind==='telegram'?'-1001234567890':kind==='email'?'ops@example.com':'https://alerts.example.com/hook'}/></label><div className="notice"><ShieldCheck size={16}/>新通道默认禁用；敏感 Provider 凭据仅允许通过服务器环境变量配置。</div><button disabled={busy||!destination}>保存禁用通道</button></form><section className="panel automation-list"><div className="panel-head"><h3>Notification Center</h3><span>Telegram · Email · Webhook</span></div>{channels.map(channel=><div className="automation-row" key={channel.id}><span className="channel-icon">{channel.kind==='telegram'?<Send/>:channel.kind==='email'?<Mail/>:<Webhook/>}</span><div><b>{channel.name}</b><small>{channel.kind} · {channel.destination}</small></div><em>{channel.enabled?'启用':'默认禁用'}</em><button disabled={busy} onClick={()=>void action('/automation/notify',{method:'POST',body:JSON.stringify({channelId:channel.id,subject:'Lightning Wallet 测试',message:'Dry Run notification',dryRun:true})})}>Dry Run</button></div>)}{!channels.length&&<p className="automation-empty">尚未配置通知通道。</p>}</section></div>}
function Jobs({jobs,busy,action}:{jobs:AutomationJob[];busy:boolean;action:(p:string,i?:RequestInit)=>Promise<void>}){return <section className="panel automation-jobs"><div className="panel-head"><h3><History size={17}/> Job History & Retry Queue</h3><span>{jobs.length} 条</span></div><div className="automation-table"><div><b>任务</b><b>类型</b><b>状态</b><b>尝试</b><b>时间</b><b>操作</b></div>{jobs.map(job=><div key={job.id}><span>{job.rule_name}</span><span>{kinds[job.kind]}</span><span className={job.status}>{job.status}{job.dry_run?' · Dry Run':''}</span><span>#{job.attempt}</span><span>{new Date(job.created_at).toLocaleString()}</span><button disabled={busy||job.status==='completed'} onClick={()=>void action(`/automation/jobs/${job.id}/retry`,{method:'POST'})}><RotateCcw size={14}/>重试</button></div>)}</div>{!jobs.length&&<p className="automation-empty">尚无执行记录。</p>}</section>}

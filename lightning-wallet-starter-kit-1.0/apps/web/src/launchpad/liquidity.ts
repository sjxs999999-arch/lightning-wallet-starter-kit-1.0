import type{LaunchDraft,LiquidityPlan}from'./types';export function planLiquidity(draft:LaunchDraft):LiquidityPlan{const tokens=BigInt(draft.liquidity.tokenAmount||'0'),quote=BigInt(draft.liquidity.quoteAmount||'0'),supply=BigInt(draft.supply||'0');if(tokens<=0n||quote<=0n||supply<=0n)throw new Error('流动性和供应量必须大于 0');if(tokens>supply)throw new Error('初始流动性 Token 数量不能超过总供应量');const scale=1_000_000n;return{initialPrice:`${quote*scale/tokens}`,tokenShareBps:Number(tokens*10_000n/supply),lockDays:draft.liquidity.lockDays,warnings:[...(draft.liquidity.lockDays<30?['LIQUIDITY_LOCK_SHORT']:[]),...(tokens*100n<supply?['LOW_INITIAL_TOKEN_LIQUIDITY']:[])]}}

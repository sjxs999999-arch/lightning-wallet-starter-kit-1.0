import{keccak256,toUtf8Bytes}from'ethers';import{validateAddress,validateAmount}from'../batch-transfer/validation';import type{TransferChain}from'../batch-transfer/types';import type{CollectorTask,ScannedAsset}from'./types';
function fixed(value:string,decimals:number){const[whole='0',fraction='']=value.split('.');return BigInt(whole||'0')*10n**BigInt(decimals)+BigInt(fraction.padEnd(decimals,'0').slice(0,decimals)||'0')}function display(value:bigint,decimals:number){const raw=value.toString().padStart(decimals+1,'0'),result=`${raw.slice(0,-decimals)||'0'}.${raw.slice(-decimals)}`;return result.replace(/\.0+$/,'').replace(/(\.\d*?)0+$/,'$1')}
export function buildCollectionPlan(chain:TransferChain,assets:ScannedAsset[],destination:string,reserve:string){if(!validateAddress(chain,destination))throw new Error('归集地址格式或校验和无效');if(reserve!=='0'&&!validateAmount(reserve))throw new Error('最低余额保留值无效');const seen=new Set<string>();return assets.map(asset=>{const key=`${chain}|${asset.address}|${asset.token??'native'}|${destination}`.toLowerCase();if(seen.has(key))throw new Error(`检测到重复归集任务：${asset.address}`);seen.add(key);const decimals=asset.decimals??(chain==='SOL'?9:chain==='TRON'?6:18),appliedReserve=asset.asset==='native'?reserve:'0',raw=fixed(asset.balance,decimals)-fixed(appliedReserve,decimals)-(asset.asset==='native'?fixed(asset.estimatedFee,decimals):0n),collectAmount=display(raw>0n?raw:0n,decimals);return{...asset,id:keccak256(toUtf8Bytes(key)),destination,reserve:appliedReserve,collectAmount,executionStatus:asset.status==='failed'?'failed':'pending',attempts:0}as CollectorTask})}
